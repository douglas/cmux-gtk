[package]
name = "ghostty-sys"
version = "0.62.0-alpha.5"
edition.workspace = true
description = "Raw FFI bindings to libghostty (embedded runtime)"

[features]
link-ghostty = []

[build-dependencies]
cc = "1"
# bindgen = "0.70"  # TODO: enable when building against actual libghostty
